- job:
    name: 'website-check'
    node: openswitch

    wrappers:
        - build-timeout:
            timeout: 60
        - timestamps
        - ansicolor

    builders:
        - revoke-sudo
        - gulp-dist:
            platform: genericx86-64

- job:
    name: 'website-gate'
    node: openswitch

    wrappers:
        - build-timeout:
            timeout: 60
        - timestamps
        - ansicolor

    builders:
        - revoke-sudo
        - gulp-dist:
            platform: genericx86-64

    publishers:
        - scp:
            site: static.openswitch.net
            files:
              - target: ''
                source: 'www/**'
                keep-hierarchy: true
                copy-after-failure: false
