- job:
    name: 'ops-switchd-check'
    node: openswitch

    wrappers:
        - build-timeout:
            timeout: 120

    builders:
        - revoke-sudo
        - gerrit-git-prep

- job:
    name: 'ops-switchd-gate'
    node: openswitch

    wrappers:
        - build-timeout:
            timeout: 120

    builders:
        - revoke-sudo
        - gerrit-git-prep
