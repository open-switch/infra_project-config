- job-template:
    name: 'ops-passwd-srv-check-{platform}'
    node: openswitch

    wrappers:
        - build-timeout:
            timeout: 120
        - timestamps
        - ansicolor

    builders:
        - revoke-sudo
        - commit-message-validation:
            module: 'ops-passwd-srv'
        - module-build-branch:
            module: 'ops-passwd-srv'
            platform: '{platform}'

- job:
    name: 'ops-passwd-srv-gate-as5712'
    node: openswitch

    wrappers:
        - build-timeout:
            timeout: 180

    builders:
        - module-build-branch:
            module: 'ops-passwd-srv'
            platform: 'as5712'
    publishers:
        - post-ws-clean

- job:
    name: 'ops-passwd-srv-gate-genericx86-64'
    node: vsi

    wrappers:
        - build-timeout:
            timeout: 180

    builders:
        - module-build-branch:
            module: 'ops-passwd-srv'
            platform: 'genericx86-64'
        - check-docker-state
        - clear-test-logs
        - module-test:
            module: 'ops-passwd-srv'
            platform: 'genericx86-64'

    publishers:
        - conditional-gather-test-results
        - conditional-htmlreports
        - archive-syslog
        - archive-test-logs-on-failure
        - post-ws-clean

- job-group:
    name: 'ops-passwd-srv-jobs'
    jobs:
      - 'ops-passwd-srv-check-{platform}'
