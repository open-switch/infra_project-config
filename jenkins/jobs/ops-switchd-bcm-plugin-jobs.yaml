- job-template:
    name: 'ops-switchd-bcm-plugin-check-{platform}'
    node: openswitch
    wrappers:
        - build-timeout:
            timeout: 120
        - timestamps
        - ansicolor
    builders:
        - revoke-sudo
        - module-build-branch:
            module: 'ops-switchd-bcm-plugin'
            platform: '{platform}'

- job:
    name: 'ops-switchd-bcm-plugin-gate-as5712'
    node: openswitch

    wrappers:
        - build-timeout:
            timeout: 120
        - timestamps
        - ansicolor

    builders:
        - module-build-branch:
            module: 'ops-switchd-bcm-plugin'
            platform: 'as5712'

- job:
    name: 'ops-switchd-bcm-plugin-gate-genericx86-64'
    node: vsi

    wrappers:
        - build-timeout:
            timeout: 240
        - timestamps
        - ansicolor

    builders:
        - module-build-branch:
            module: 'ops-switchd-bcm-plugin'
            platform: 'genericx86-64'
        - module-cttest:
            module: 'ops-switchd-bcm-plugin'
            platform: 'genericx86-64'

    publishers:
        - junit:
            results: 'build/*test*.xml'

- job-group:
    name: 'ops-switchd-bcm-plugin-jobs'
    jobs:
      - 'ops-switchd-bcm-plugin-check-{platform}'
