- job-template:
    name: 'lldpd-check-{platform}'
    node: openhalon

    wrappers:
        - build-timeout:
            timeout: 120
        - timestamps
        - ansicolor

    builders:
        - revoke-sudo
        - module-build:
            module: 'lldpd'
            platform: '{platform}'

- job-template:
    name: 'lldpd-gate-{platform}'
    node: openhalon

    wrappers:
        - build-timeout:
            timeout: 120
        - timestamps
        - ansicolor

    builders:
        - revoke-sudo
        - module-build:
            module: 'lldpd'
            platform: '{platform}'

- job-group:
    name: 'lldpd-jobs'
    jobs:
      - 'lldpd-check-{platform}'
      - 'lldpd-gate-{platform}'