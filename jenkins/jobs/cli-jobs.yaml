- job-template:
    name: 'cli-check-{platform}'
    node: openhalon

    wrappers:
        - build-timeout:
            timeout: 120
        - timestamps
        - ansicolor

    builders:
        - revoke-sudo
        - module-build:
            module: 'cli'
            platform: '{platform}'

- job-template:
    name: 'cli-gate-{platform}'
    node: openhalon

    wrappers:
        - build-timeout:
            timeout: 120
        - timestamps
        - ansicolor

    builders:
        - revoke-sudo
        - module-build:
            module: 'cli'
            platform: '{platform}'

- job-group:
    name: 'cli-jobs'
    jobs:
      - 'cli-check-{platform}'
      - 'cli-gate-{platform}'
