- job-template:
    name: 'ops-switchd-sim-plugin-check-{platform}'
    node: openswitch
    wrappers:
        - build-timeout:
            timeout: 120
        - timestamps
        - ansicolor
    builders:
        - revoke-sudo
        - module-build-branch:
            module: 'ops-switchd-sim-plugin'
            platform: '{platform}'

- job:
    name: 'ops-switchd-sim-plugin-gate-as5712'
    node: openswitch

    wrappers:
        - build-timeout:
            timeout: 120
        - timestamps
        - ansicolor

    builders:
        - module-build-branch:
            module: 'ops-switchd-sim-plugin'
            platform: 'as5712'

- job:
    name: 'ops-switchd-sim-plugin-gate-genericx86-64'
    node: vsi

    wrappers:
        - build-timeout:
            timeout: 240
        - timestamps
        - ansicolor

    builders:
        - module-build-branch:
            module: 'ops-switchd-sim-plugin'
            platform: 'genericx86-64'
        - module-cttest-trap:
            module: 'ops-switchd-sim-plugin'
            platform: 'genericx86-64'

    publishers:
        - junit:
            results: 'build/*test*.xml'

- job-group:
    name: 'ops-switchd-sim-plugin-jobs'
    jobs:
      - 'ops-switchd-sim-plugin-check-{platform}'
