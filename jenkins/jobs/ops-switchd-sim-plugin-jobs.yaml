- job-template:
    name: 'ops-switchd-sim-plugin-check-{platform}'
    node: openswitch

    wrappers:
        - build-timeout:
            timeout: 120
        - timestamps
        - ansicolor

    builders:
        - revoke-sudo
        - module-build-branch:
            module: 'ops-switchd-sim-plugin'
            platform: '{platform}'

- job-template:
    name: 'ops-switchd-sim-plugin-gate-{platform}'
    node: openswitch

    wrappers:
        - build-timeout:
            timeout: 120
        - timestamps
        - ansicolor

    builders:
        - revoke-sudo
        - module-build-branch:
            module: 'ops-switchd-sim-pluginn'
            platform: '{platform}'

- job-group:
    name: 'ops-switchd-sim-plugin-jobs'
    jobs:
      - 'ops-switchd-sim-plugin-check-{platform}'
      - 'ops-switchd-sim-plugin-gate-{platform}'
