- job-template:
    name: 'halonutils-check-{platform}'
    node: openhalon

    wrappers:
        - build-timeout:
            timeout: 120
        - timestamps
        - ansicolor

    builders:
        - revoke-sudo
        - module-build:
            module: 'halonutils'
            platform: '{platform}'

- job-template:
    name: 'halonutils-gate-{platform}'
    node: openhalon

    wrappers:
        - build-timeout:
            timeout: 120
        - timestamps
        - ansicolor

    builders:
        - revoke-sudo
        - module-build:
            module: 'halonutils'
            platform: '{platform}'

- job-group:
    name: 'halonutils-jobs'
    jobs:
      - 'halonutils-check-{platform}'
      - 'halonutils-gate-{platform}'
