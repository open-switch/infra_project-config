- job-template:
    name: 'cfgd-check-{platform}'
    node: openhalon

    wrappers:
        - build-timeout:
            timeout: 120
        - timestamps
        - ansicolor

    builders:
        - revoke-sudo
        - module-build:
            module: 'cfgd'
            platform: '{platform}'

- job-template:
    name: 'cfgd-gate-{platform}'
    node: openhalon

    wrappers:
        - build-timeout:
            timeout: 120
        - timestamps
        - ansicolor

    builders:
        - revoke-sudo
        - module-build:
            module: 'cfgd'
            platform: '{platform}'

- job-group:
    name: 'cfgd-jobs'
    jobs:
      - 'cfgd-check-{platform}'
      - 'cfgd-gate-{platform}'