- job-template:
    name: 'vsi-check-{platform}'
    node: openhalon

    wrappers:
        - build-timeout:
            timeout: 120
        - timestamps
        - ansicolor

    builders:
        - revoke-sudo
        - module-build:
            module: 'halon-vsi'
            platform: '{platform}'

- job-template:
    name: 'vsi-gate-{platform}'
    node: openhalon

    wrappers:
        - build-timeout:
            timeout: 120
        - timestamps
        - ansicolor

    builders:
        - revoke-sudo
        - module-build:
            module: 'halon-vsi'
            platform: '{platform}'

- job-group:
    name: 'vsi-jobs'
    jobs:
      - 'vsi-check-{platform}'
      - 'vsi-gate-{platform}'
