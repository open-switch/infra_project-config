- job-template:
    name: 'sysd-check-{platform}'
    node: openswitch

    wrappers:
        - build-timeout:
            timeout: 120
        - timestamps
        - ansicolor

    builders:
        - revoke-sudo
        - module-build:
            module: 'ops-sysd'
            platform: '{platform}'

- job-template:
    name: 'sysd-gate-{platform}'
    node: openswitch

    wrappers:
        - build-timeout:
            timeout: 120
        - timestamps
        - ansicolor

    builders:
        - revoke-sudo
        - module-build:
            module: 'ops-sysd'
            platform: '{platform}'

- job-group:
    name: 'ops-sysd-jobs'
    jobs:
      - 'ops-sysd-check-{platform}'
      - 'ops-sysd-gate-{platform}'