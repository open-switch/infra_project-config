- job:
    name: 'ops-lldpd-check-as5712'
    node: openswitch

    wrappers:
        - build-timeout:
            timeout: 120
        - timestamps
        - ansicolor

    builders:
        - module-build-branch:
            module: 'ops-lldpd'
            platform: 'as5712'

- job:
    name: 'ops-lldpd-check-genericx86-64'
    node: openswitch

    wrappers:
        - build-timeout:
            timeout: 120
        - timestamps
        - ansicolor

    builders:
        - module-build-branch:
            module: 'ops-lldpd'
            platform: 'genericx86-64'

- job:
    name: 'ops-lldpd-gate-as5712'
    node: openswitch

    wrappers:
        - build-timeout:
            timeout: 120
        - timestamps
        - ansicolor

    builders:
        - module-build-branch:
            module: 'ops-lldpd'
            platform: 'as5712'

- job:
    name: 'ops-lldpd-gate-genericx86-64'
    node: vsi

    wrappers:
        - build-timeout:
            timeout: 240
        - timestamps
        - ansicolor

    builders:
        - module-build-branch:
            module: 'ops-lldpd'
            platform: 'genericx86-64'
        - module-cttest:
            module: 'ops-lldpd'
            platform: 'genericx86-64'

    publishers:
        - junit:
            results: 'build/*test*.xml'
