includes:
  - python-file: openstack_functions.py

pipelines:
  - name: check
    description: Newly uploaded patchsets enter this pipeline to receive an initial +/-1 Verified vote from Jenkins.
    success-message: Build succeeded (check pipeline).
    failure-message: Build failed (check pipeline).  
    manager: IndependentPipelineManager
    source: gerrit
    precedence: normal
    require:
      open: True
      current-patchset: True
    trigger:
      gerrit:
        - event: patchset-created
        - event: change-restored
        - event: comment-added
          comment: (?i)^(Patch Set [0-9]+:)?( [\w\\+-]*)*(\n\n)?\s*(recheck|reverify)
        - event: comment-added
          require-approval:
            - verified: [-1, -2]
              username: zuul 
          approval:
            - workflow: 1
    success:
      gerrit:
        verified: 1
    failure:
      gerrit:
        verified: -1

  - name: gate
    description: Changes that have been approved by core developers are enqueued in order in this pipeline, and if they pass tests in Jenkins, will be merged.
    success-message: Build succeeded (gate pipeline).
    failure-message: Build failed (gate pipeline).  
    manager: DependentPipelineManager
    source: gerrit
    precedence: high
    require:
      open: True
      current-patchset: True
      approval:
        - verified: [1, 2]
          username: zuul 
        - workflow: 1
    trigger:
      gerrit:
        - event: comment-added
          approval:
            - workflow: 1
        - event: comment-added
          approval:
            - code-review: 2
        - event: comment-added
          approval:
            - verified: 1
          username: zuul 
    start:
      gerrit:
        verified: 0
    success:
      gerrit:
        verified: 2
        submit: true
    failure:
      gerrit:
        verified: -2
    window-floor: 20
    window-increase-factor: 2

  - name: post
    description: This pipeline runs jobs that operate after each change is merged.
    manager: IndependentPipelineManager
    source: gerrit
    precedence: low
    trigger:
      gerrit:
        - event: ref-updated
          ref: ^(?!refs/).*$

  - name: tag
    description: This pipeline runs jobs in response to any tag event.
    manager: IndependentPipelineManager
    source: gerrit
    precedence: high
    trigger:
      gerrit:
        - event: ref-updated
          ref: ^refs/tags/.*$

  - name: pre-release
    description: This pipeline runs jobs on projects in response to pre-release tags.
    manager: IndependentPipelineManager
    source: gerrit
    precedence: high
    trigger:
      gerrit:
        - event: ref-updated
          ref: ^refs/tags/[0-9]+(\.[0-9]+)*(a|b|rc)[0-9]+$

  - name: release
    description: When a commit is tagged as a release, this pipeline runs jobs that publish archives and documentation.
    manager: IndependentPipelineManager
    source: gerrit
    precedence: high
    trigger:
      gerrit:
        - event: ref-updated
          ref: ^refs/tags/[0-9]+(\.[0-9]+)*$

  - name: silent
    description: This pipeline is used for silently testing new jobs.
    manager: IndependentPipelineManager
    source: gerrit
    precedence: low
    trigger:
      gerrit:
        - event: patchset-created
        - event: comment-added
          comment: (?i)^(Patch Set [0-9]+:)?( [\w\\+-]*)*(\n\n)?\s*(recheck|reverify)

  - name: experimental
    description: On-demand pipeline for requesting a run against a set of jobs that are not yet gating. Leave review comment of "check experimental" to run jobs in this pipeline.
    success-message: Build succeeded (experimental pipeline).
    failure-message: Build failed (experimental pipeline).
    manager: IndependentPipelineManager
    source: gerrit
    precedence: normal
    trigger:
      gerrit:
        - event: comment-added
          comment: (?i)^(Patch Set [0-9]+:)?( [\w\\+-]*)*(\n\n)?\s*check experimental\s*$
    success:
      gerrit: {}
    failure:
      gerrit: {}

  - name: periodic
    description: Jobs in this queue are triggered on a timer.
    manager: IndependentPipelineManager
    source: gerrit
    precedence: low
    trigger:
      timer:
        - time: '0 */6 * * *'

  - name: periodic-stable
    description: Periodic checks of the stable branches.
    manager: IndependentPipelineManager
    source: gerrit
    precedence: low
    trigger:
      timer:
        - time: '1 6 * * *'
    failure:
      smtp:
        from: jenkins@openstack.org
        to: openstack-stable-maint@lists.openstack.org
        subject: 'Stable check of {change.project} failed'

  - name: periodic-qa
    description: Periodic jobs for the QA team.
    manager: IndependentPipelineManager
    source: gerrit
    precedence: low
    trigger:
      timer:
        - time: '2 6 * * *'
    failure:
      smtp:
        from: jenkins@openstack.org
        to: openstack-qa@lists.openstack.org
        subject: 'Periodic jobs for {change.project} failed'

  - name: merge-check
    description: >
      Each time a change merges, this pipeline verifies that all open changes
      on the same project are still mergeable.
    failure-message: Build failed (merge-check pipeline).
    manager: IndependentPipelineManager
    ignore-dependencies: true
    source: gerrit
    precedence: low
    require:
      approval:
        - verified: [1, 2]
          username: zuul
    trigger:
      zuul:
        - event: project-change-merged
    merge-failure:
      gerrit:
        verified: -1

project-templates:
  - name: merge-check
    merge-check:
      - noop

  - name: noop-jobs
    check:
      - noop
    gate:
      - noop

  - name: openhalon-build
    check:
      - 'openhalon-check-{platform}'
    gate:
      - 'openhalon-gate-{platform}'

  - name: module-build
    check:
      - '{module}-check-{platform}'
    gate:
      - '{module}-gate-{platform}'

projects:
  - name: openhalon/openhalon 
    template:
      - name: merge-check
      - name: openhalon-build
        platform: genericx86-64
      - name: openhalon-build
        platform: as5712
    periodic:
      - openhalon-periodic-genericx86-64
      - openhalon-periodic-as5712

  - name: openhalon/halonutils
    template:
      - name: merge-check
      - name: module-build
        platform: genericx86-64
        module: halonutils
      - name: module-build
        module: halonutils
        platform: as5712

  - name: openhalon/config-yaml
    template:
      - name: merge-check
      - name: module-build
        platform: genericx86-64
        module: config-yaml

  - name: openhalon/openvswitch
    template:
      - name: merge-check
      - name: module-build
        module: openvswitch
        platform: genericx86-64
      - name: module-build
        module: openvswitch
        platform: as5712

  - name: openhalon/cfgd
    template:
      - name: merge-check
      - name: module-build
        module: cfgd
        platform: genericx86-64

  - name: openhalon/fand
    template:
      - name: merge-check
      - name: module-build
        module: fand
        platform: genericx86-64
      - name: module-build
        module: fand
        platform: as5712

  - name: openhalon/intfd
    template:
      - name: merge-check
      - name: module-build
        module: intfd
        platform: genericx86-64

  - name: openhalon/lacpd
    template:
      - name: merge-check
      - name: module-build
        module: lacpd
        platform: genericx86-64
      - name: module-build
        module: lacpd
        platform: as5712

  - name: openhalon/ledd
    template:
      - name: merge-check
      - name: module-build
        module: ledd
        platform: genericx86-64
      - name: module-build
        module: ledd
        platform: as5712

  - name: openhalon/lldpd
    template:
      - name: merge-check
      - name: module-build
        module: lldpd
        platform: genericx86-64

  - name: openhalon/pmd
    template:
      - name: merge-check
      - name: module-build
        module: pmd
        platform: genericx86-64
      - name: module-build
        module: pmd
        platform: as5712

  - name: openhalon/powerd
    template:
      - name: merge-check
      - name: module-build
        module: powerd
        platform: genericx86-64
      - name: module-build
        module: powerd
        platform: as5712

  - name: openhalon/sysd
    template:
      - name: merge-check
      - name: module-build
        module: sysd
        platform: genericx86-64
      - name: module-build
        module: sysd
        platform: as5712

  - name: openhalon/tempd
    template:
      - name: merge-check
      - name: module-build
        module: tempd
        platform: genericx86-64
      - name: module-build
        module: tempd
        platform: as5712

  - name: openhalon/quagga
    template:
      - name: merge-check
      - name: module-build
        module: quagga
        platform: genericx86-64

  - name: openhalon/as5712-config
    template:
      - name: merge-check
      - name: module-build
        module: as5712-config
        platform: genericx86-64

  - name: openhalon/ovs-sim-plugin
    template:
      - name: merge-check
      - name: module-build
        module: ovs-sim-plugin
        platform: genericx86-64

  - name: openhalon/aaa-utils
    template:
      - name: merge-check
      - name: module-build
        module: aaa-utils
        platform: genericx86-64

  - name: openhalon/simulator-config
    template:
      - name: merge-check
      - name: module-build
        module: simulator-config
        platform: genericx86-64

  - name: openhalon/governance
    template:
      - name: merge-check
    check:
      - gate-governance-docs
    gate:
      - gate-governance-docs
    post:
      - static-governance-publish

  - name: openhalon/vland
    template:
      - name: merge-check
      - name: module-build
        module: vland
        platform: genericx86-64
      - name: module-build
        module: vland
        platform: as5712

  - name: openhalon/arpmgrd
    template:
      - name: merge-check
      - name: module-build
        module: arpmgrd
        platform: genericx86-64
      - name: module-build
        module: arpmgrd
        platform: as5712

  - name: openhalon/restd
    template:
      - name: merge-check
      - name: module-build
        module: restd
        platform: genericx86-64
      - name: module-build
        module: restd
        platform: as5712

  - name: openhalon/vsi
    template:
      - name: merge-check
      - name: module-build
        module: vsi
        platform: genericx86-64
      - name: module-build
        module: vsi
        platform: as5712

  - name: openhalon/cli
    template:
      - name: merge-check
      - name: module-build
        module: cli
        platform: genericx86-64
      - name: module-build
        module: cli
        platform: as5712